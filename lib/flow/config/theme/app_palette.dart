import 'package:flutter/material.dart';

/// A centralized class that holds all the color constants used throughout the app.
/// This promotes consistency and easy theming across the application UI.
class AppPalette {
  /// Main brand color of the app (used in AppTheme as primaryColor)
  /// Hex: #29D177 (a fresh green tone)
  static const mainColor  = Color(0xFF29D177);
  static const grayColor  = Color(0xFFDEDEDE);
  static const greenColor = Color(0xFF06C25E);
  static const redColor   = Color(0xFFFF0000);
  static const orengeColor= Color(0xFFFFA115);
  static const blackColor = Colors.black;
  static const whiteColor = Colors.white;
  static const blueColor  = Colors.blue;
}
